import React from "react";

// Define a functional component named Users
const Users = () => {
  // Return JSX that represents the component's UI
  return <h2>Users</h2>;
};

// Export the Users component so it can be imported and used in other files
export default Users;
